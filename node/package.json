{
  "name": "node",
  "version": "1.0.0",
  "description": "不能建在egret项目的跟目录下，会跟egret的配置冲突",
  "main": "carry.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kone",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-strip-debug": "^3.0.0",
    "gulp-uglify": "^3.0.0"
  }
}
